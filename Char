#include<bits/stdc++.h>
using namespace std;
int main()
{
    char x;
    cin >> x;

    if(x == toupper(x))
    {
        x = x + 32;
        cout << char(x) << endl;
    }
    else if(x == tolower(x))
    {
        x = x - 32;
        cout << char(x) << endl;
    }

    return 0;
}
