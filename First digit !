#include<bits/stdc++.h>
using namespace std;
int main()
{
    int x, test;
    cin >> x;
    test = (x / 1000);

    if(test % 2 == 0)
        cout << "EVEN" << endl;
    else if(test % 2 != 0)
        cout << "ODD" << endl;

    return 0;
}
