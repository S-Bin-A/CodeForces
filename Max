#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n, i;
    cin >> n;

    long long int x, maxi = 0;

    for(i = 1; i <= n; i++)
    {
        cin >> x;

        if(maxi < x)
            maxi = x;
    }

    cout << maxi << endl;

    return 0;
}
