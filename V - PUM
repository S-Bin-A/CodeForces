#include<bits/stdc++.h>
using namespace std;
 
int main()
{
    int n, i;
    cin >> n;
 
    for(i = 1; i <= n*4; i++)
    {
        if(i % 4 != 0)
            cout << i << " ";
        else
            cout << "PUM" << endl;
    }
 
    return 0;
}
