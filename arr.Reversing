#include<bits/stdc++.h>
using namespace std;
 
int main()
{
    long long int n, i;
    cin >> n;
    vector <long long int> a(n);
 
    for(i = 0; i < n; i++)
    {
        cin >> a[i];
    }
 
    reverse(a.begin(), a.end());
 
    for(i = 0; i < n; i++)
    {
        cout << a[i] << " ";
    }
 
    cout << endl;
 
    return 0;
}
