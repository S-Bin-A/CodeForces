#include<bits/stdc++.h>
using namespace std;
int main()
{
    char s;
    int n, x, i, j;
    cin >> s >> n;

    for(i = 1; i <= n; i++)
    {
        cin >> x;

        for(j = 1; j <= x; j++)
        {
            cout << s;
        }

        cout << endl;
    }

    return 0;
}
